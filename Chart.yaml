apiVersion: v2
name: sovereign-ai-gateway
description: A Helm chart for deploying the Sovereign AI stack (LiteLLM Gateway and vLLM Inference Engine)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: kube-prometheus-stack
    version: "61.3.0" # Or whichever stable recent version
    repository: "https://prometheus-community.github.io/helm-charts"
    condition: observability.enabled
