# Default values for sovereign-ai-gateway.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

litellm:
  image:
    repository: ghcr.io/berriai/litellm
    pullPolicy: IfNotPresent
    # Overrides the image tag whose default is the chart appVersion.
    tag: "main-v1.41.0"
  replicaCount: 1
  service:
    type: LoadBalancer
    port: 4000
  # Vendor-specific IdP hooks go here, completely decoupled from the chart logic.
  extraEnv:
    - name: ENABLE_SSO
      value: "true"
    # Example: If the client uses Azure
    - name: AZURE_CLIENT_ID
      value: "placeholder-azure-client-id"
    - name: AZURE_TENANT_ID
      value: "placeholder-azure-tenant-id"
    # Example: If the client uses Okta
    # - name: OKTA_DOMAIN
    #   value: "..."

  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
    # fsGroup: 1000

  securityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
      - ALL

  networkPolicy:
    enabled: true

  probes:
    livenessProbe:
      httpGet:
        path: /health
        port: http
      initialDelaySeconds: 15
      periodSeconds: 20
    readinessProbe:
      httpGet:
        path: /health/readiness
        port: http
      initialDelaySeconds: 5
      periodSeconds: 10

  pdb:
    create: true
    minAvailable: 1

  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
              - sovereign-ai-gateway-litellm
          topologyKey: kubernetes.io/hostname

  serviceMonitor:
    enabled: true
    path: /metrics

sharedSecret:
  enabled: true
  token: "sk-sovereign-ai-internal-admin-token"

backends:
  - name: "guardrail"
    modelName: "facebook/opt-125m"
    device: "auto"
    replicaCount: 1
    image:
      repository: vllm/vllm-openai
      pullPolicy: IfNotPresent
      tag: "latest"
    service:
      type: ClusterIP
      port: 8000
    resources:
      limits:
        cpu: "2"
        memory: "4Gi"
      requests:
        cpu: "1"
        memory: "2Gi"
    persistence:
      enabled: false
      size: 10Gi
      storageClass: ""
      accessMode: ReadWriteOnce
    podSecurityContext:
      runAsNonRoot: true
      runAsUser: 1000
    securityContext:
      allowPrivilegeEscalation: false
      readOnlyRootFilesystem: true
      capabilities:
        drop:
        - ALL
    extraArgs: []
    probes:
      startupProbe:
        httpGet:
          path: /health
          port: http
        initialDelaySeconds: 30
        periodSeconds: 10
        failureThreshold: 60
      livenessProbe:
        httpGet:
          path: /health
          port: http
        initialDelaySeconds: 15
        periodSeconds: 20
      readinessProbe:
        httpGet:
          path: /health
          port: http
        initialDelaySeconds: 5
        periodSeconds: 10
    pdb:
      create: true
      minAvailable: 1
    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          podAffinityTerm:
            labelSelector:
              matchExpressions:
              - key: app.kubernetes.io/name
                operator: In
                values:
                - sovereign-ai-gateway-vllm-guardrail
            topologyKey: kubernetes.io/hostname
    networkPolicy:
      enabled: true
    serviceMonitor:
      enabled: true
      path: /metrics

postgresql:
  enabled: true
  instances: 1
  image: "ghcr.io/cloudnative-pg/postgresql:16.4"
  auth:
    dbPassword: "secure-app-password"
  resources:
    limits:
      cpu: "1"
      memory: "1Gi"
    requests:
      cpu: "500m"
      memory: "512Mi"
  persistence:
    size: 20Gi
    storageClass: ""
  networkPolicy:
    enabled: true

openwebui:
  enabled: true
  image:
    repository: ghcr.io/open-webui/open-webui
    tag: "main"
    pullPolicy: IfNotPresent
  replicaCount: 1
  service:
    type: ClusterIP
    port: 8080
  resources:
    limits:
      cpu: "2"
      memory: "2Gi"
    requests:
      cpu: "1"
      memory: "1Gi"
  hpa:
    enabled: true
    minReplicas: 1
    maxReplicas: 5
    targetCPUUtilizationPercentage: 80
    targetMemoryUtilizationPercentage: 80
  networkPolicy:
    enabled: true
  podSecurityContext:
    runAsNonRoot: true
    runAsUser: 1000
  securityContext:
    allowPrivilegeEscalation: false
    readOnlyRootFilesystem: true
    capabilities:
      drop:
      - ALL
  probes:
    livenessProbe:
      httpGet:
        path: /health
        port: http
      initialDelaySeconds: 15
      periodSeconds: 20
    readinessProbe:
      httpGet:
        path: /health
        port: http
      initialDelaySeconds: 10
      periodSeconds: 10
  pdb:
    create: true
    minAvailable: 1
  affinity:
    podAntiAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchExpressions:
            - key: app.kubernetes.io/name
              operator: In
              values:
              - sovereign-ai-gateway-openwebui
          topologyKey: kubernetes.io/hostname
  serviceMonitor:
    enabled: true
    path: /metrics

observability:
  enabled: true
  # Values propagated to the kube-prometheus-stack sub-chart
  kube-prometheus-stack:
    grafana:
      enabled: true
    prometheus:
      prometheusSpec:
        serviceMonitorSelectorNilUsesHelmValues: false
